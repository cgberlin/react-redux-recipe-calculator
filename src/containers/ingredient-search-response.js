import React, { Component, PropTypes } from 'react'
import { connect } from 'react-redux'
import store from '../index'

const SearchResponse = React.createClass({
  render : () => {
    return (
      <div>
      Response
      </div>
    );
  }
});

export default SearchResponse;
