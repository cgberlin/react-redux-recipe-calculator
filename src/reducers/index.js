import { combineReducers } from 'redux';
import {submitRecipe, SUBMIT_RECIPE, fetchByIngredients} from '../actions/index'

function rootReducer(state = {}, action) {

      return state;
}

export default rootReducer;
